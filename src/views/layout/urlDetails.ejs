<div class="url-details-container" id="url-details-container" style="margin-top: 100px;">
    <!-- Hero Section -->
    <section class="hero">
        <p><strong>Analíticas de URL</strong> Información detallada sobre el rendimiento de tus enlaces.</p>
    </section>

    <!-- Header Card -->
    <div class="card url-header-card details-header-container">
        <div class="url-header-info">
            <h1 class="short-url-title" id="detail-short-url">Cargando...
                <span class="badge active" id="detail-status" style="display:none;">Activo</span>
                <span class="badge protected" id="detail-protected" style="display:none;">Protegido</span>
                <span class="badge expired" id="detail-expired" style="display:none;">Expirado</span>
            </h1>
            <p class="long-url-text" id="detail-long-url"><i class="fa-solid fa-link"></i> Cargando...</p>
        </div>
        <div class="header-actions" style="display: flex; flex-direction: column; align-items: center; gap: 10px;">
            <a class="btn-action" id="btn-customize-url" href="#customize-url-section"
                style="width: 100%; background: var(--bg-primary); border: 1px solid var(--border-color); color: var(--text-primary);">
                <i class="fa-solid fa-gear"></i> <span style="width: 100%; text-align: center;">Personalizar</span>
            </a>
            <a class="btn-action" id="btn-toggle-status"
                style="width: 100%; background: var(--bg-primary); border: 1px solid var(--border-color); color: var(--text-primary);">
                <i class="fa-solid fa-power-off"></i> <span style="width: 100%; text-align: center;">Desactivar</span>
            </a>
            <a class="btn-action" style="background: var(--accent-color); color: #000;" id="btn-copy-detail">
                <i class="fa-regular fa-copy"></i> <span style="width: 100%; text-align: center;">Copiar</span>
            </a>
        </div>
    </div>
    <!-- Main Grid -->
    <div class="url-details-grid">
        <!-- Left Column: Click Performance -->
        <div class="card click-performance-card">
            <div class="card-header">
                <h3 class="card-title">Rendimiento de Clicks</h3>
                <div class="time-filters">
                    <button class="filter-btn active">30D de historial</button>
                </div>
            </div>
            <div class="chart-container">
                <canvas id="detail-chart"></canvas>
            </div>
            <h3 class="card-title" style="margin-top: 20px;">Análisis</h3>
            <div id="description-analitycs"></div>

            <h3 class="card-title"
                style="margin-top: 20px; border-top: 1px solid var(--border-color); padding-top:20px;">Ubicaciones</h3>
            <div class="locations-list" id="detail-locations-list" style="margin-top: 15px;">
                <!-- Las localizaciones se renderizan aqui -->
                <p style="text-align: center; color: var(--text-secondary);">Cargando países...</p>
            </div>
        </div>

        <!-- Right Column -->
        <div class="right-column-grid">
            <!-- QR Code Card -->
            <div class="card qr-code-card text-center">
                <h3 class="card-title text-left">Código QR</h3>
                <div class="qr-wrapper flex-center">
                    <img id="detail-qr-img" src="" alt="QR Code"
                        style="display: none; width: 140px; height: 140px; background: white; padding: 10px; border-radius: 8px;">
                    <div id="qr-loading">Cargando...</div>
                </div>
                <button class="btn-download-qr" id="btn-download-qr">DESCARGAR QR</button>
            </div>
            <!-- Stats Mini Cards -->
            <div class="card mini-stat-card">
                <div class="stat-info">
                    <span class="stat-label">ULTIMAS 24 HORAS</span>
                    <h2 class="stat-value" id="detail-last-24">+0</h2>
                </div>
                <div class="stat-icon click-icon">
                    <i class="fa-solid fa-bolt"></i>
                </div>
            </div>

            <div class="card mini-stat-card">
                <div class="stat-info">
                    <span class="stat-label">TOTAL DE CLICKS</span>
                    <h2 class="stat-value" id="detail-total-clicks">0</h2>
                </div>
                <div class="stat-icon click-icon">
                    <i class="fa-solid fa-hand-pointer"></i>
                </div>
            </div>

            <div class="card mini-stat-card">
                <div class="stat-info">
                    <span class="stat-label">CREADO</span>
                    <h2 class="stat-value" id="detail-created-date">-</h2>
                </div>
                <div class="stat-icon calendar-icon">
                    <i class="fa-regular fa-calendar"></i>
                </div>
            </div>
        </div>
    </div>
    <div class="card" id="customize-url-section">
        <div class="card-header">
            <h3 class="card-title">Personalizar</h3>
        </div>
        <form id="form-customize-url">
            <div style="margin-bottom: 15px; display: flex; align-items: center; justify-content: space-between;">
                <label for="custom-url-check" style="color: var(--text-secondary);">Alias personalizado?</label>
                <input type="checkbox" id="custom-url-check">
            </div>
            <input type="text" id="custom-url-input" placeholder="Alias personalizado"
                style="display: none; width: 100%; margin-bottom: 15px; padding: 10px; background: var(--bg-primary); border: 1px solid var(--border-color); color: var(--text-primary); border-radius: 6px;">

            <div style="margin-bottom: 15px; display: flex; align-items: center; justify-content: space-between;">
                <div>
                    <label for="password-check" style="color: var(--text-secondary);">Proteger con contraseña?</label>
                    <button type="btn" id="btn-unprotect" class="badge not-protected"
                        style="display: none; border: none; cursor: pointer; margin-left:10px; padding: 5px; background: var(--accent-color); color: var(--text-primary); border-radius: 6px;">Desproteger</button>
                </div>
                <input type="checkbox" id="password-check">
            </div>
            <input type="text" id="password-input" placeholder="Contraseña"
                style="display: none; width: 100%; margin-bottom: 15px; padding: 10px; background: var(--bg-primary); border: 1px solid var(--border-color); color: var(--text-primary); border-radius: 6px;">

            <div style="margin-bottom: 15px; display: flex; align-items: center; justify-content: space-between;">
                <div>
                    <label for="expiration-date-check" style="color: var(--text-secondary);">Fecha de
                        expiración?</label>
                    <button type="btn button" id="btn-remove-expiration" class="badge not-expired"
                        style="display: none; border: none; cursor: pointer; margin-left:10px; padding: 5px; background: var(--accent-color); color: var(--text-primary); border-radius: 6px;">Quitar
                        Fecha</button>
                </div>
                <input type="checkbox" id="expiration-date-check">
            </div>
            <input type="date" id="expiration-date-input"
                style="display: none; width: 100%; margin-bottom: 15px; padding: 10px; background: var(--bg-primary); border: 1px solid var(--border-color); color: var(--text-primary); border-radius: 6px;">

            <button type="submit" class="btn-scan"
                style="width: 50%; display: flex; justify-content: center; margin: 0 auto;">
                <i class="fa-solid fa-arrow-right" aria-hidden="true"></i> Actualizar
            </button>
        </form>
    </div>
    <!-- Bottom Actions -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Eliminar URL</h3>
        </div>
        <p style="margin-bottom: 15px;">Eliminar la URL del sistema</p>
        <button class="btn-action danger" id="btn-delete-detail"><i class="fa-solid fa-trash"></i> Eliminar
            URL</button>
    </div>
</div>

<script src="/js/apiUrlDetails.js"></script>