import { Request, Response } from "express";

export const getNotificationsController = (req: Request, res: Response) => {
    
};

// disparador de notificaciones de cantidad de clicks a un link (100, 500, 1000, 2000, 5000, 10000)
export const createNotificationController = (req: Request, res: Response) => {

};